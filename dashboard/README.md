# Integration and dashboard module

**Owner:** Member 4

This module will contain integration utilities, status/metrics display work, repeatable test support, and final demo evidence.

It must show or log the device state, wake confidence, recognized text, errors, model/resource metrics, and latency when those fields become available. Do not build optional UI features before the core wake -> stream -> ASR path is proven.
